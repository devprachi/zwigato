import React from "react";
import { createRoot } from "react-dom/client";

const heading = <h1>Food Ordering App</h1>;

const root = createRoot(document.getElementById("root"));
root.render(heading);
